# Timeline可视化导出功能

## 已完成功能

### 1. 单圈导出模式 - Timeline交互
- **右键点击圈数标签**即可选择该圈用于导出
- 选中的圈会显示橙色高亮和"EXPORT"标记
- 视频轨道上会显示橙色导出范围框，清晰标注要导出的片段
- 右侧设置面板会同步显示已选择的圈数和时长

#### 操作方式
1. 在右侧设置面板选择"导出范围" → "单圈"
2. 在时间轴底部的圈数标签上**右键点击**要导出的圈（比如Lap3）
3. 该圈会变成橙色，视频轨道上会显示导出范围
4. 点击"导出带HUD的视频"开始导出

#### 视觉反馈
- **圈数标签**：选中圈显示橙色背景 + 橙色光圈 + "EXPORT"小标签
- **视频轨道**：橙色高亮框标注导出片段范围
- **设置面板**：显示"已选择：第X圈 XX.XXs"

### 2. 整个视频导出模式
- 默认模式，导出完整视频
- 无需额外操作

### 3. 自定义范围导出模式（规划中）
- UI已预留提示："💡 在时间轴上拖动范围选择器（即将推出）"
- 后续会添加类似视频编辑软件的范围拖动功能

## 技术实现

### 状态管理
- **App.tsx**: 新增`exportRangeSelection`状态管理导出范围选择
- 状态包含：
  - `mode`: 'full' | 'lap' | 'custom'
  - `selectedLapNum`: 选中的圈号
  - `customStartSec` / `customEndSec`: 自定义时间范围

### Timeline组件增强
- 新增props：`exportRangeSelection` 和 `onExportRangeChange`
- LapChips组件支持：
  - 右键点击选择圈号
  - 橙色高亮显示选中状态
  - 显示"EXPORT"标记
- 视频轨道上叠加橙色导出范围框（单圈模式时）

### SettingsPanel同步
- 移除下拉选择框，改为显示已选择的圈信息
- 添加操作提示引导用户使用Timeline交互
- 保持与Timeline的双向同步

## 用户体验提升

### Before（旧版）
❌ 需要在右侧面板下拉框中查找圈号
❌ 看不到导出范围在时间轴上的位置
❌ 数字输入不够直观

### After（新版）
✅ 直接在时间轴上右键点击圈数标签
✅ 视频轨道显示橙色导出范围
✅ 所见即所得，导出范围一目了然
✅ 交互更符合视频编辑软件习惯

## 后续优化方向

1. **自定义范围拖动器**
   - 在时间轴上添加可拖动的In/Out点
   - 类似Premiere/Final Cut的范围选择
   - 实时预览选择的时间段

2. **键盘快捷键**
   - I/O键标记In/Out点
   - 数字键快速跳转到对应圈

3. **导出预览**
   - 鼠标悬停在圈数标签上预览该圈首帧
   - 导出前预览第一帧和最后一帧
